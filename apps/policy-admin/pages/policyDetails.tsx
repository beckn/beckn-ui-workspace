import ViewInformation from '@components/viewDetails/viewDetails'
import React, { useMemo, useState } from 'react'

const PolicyDetails = () => {
  return <ViewInformation />
}

export default PolicyDetails
